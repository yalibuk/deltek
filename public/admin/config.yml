# Deltek içerik paneli — Sveltia CMS
# Yerelde "npm run cms" ile çalışır (local_backend); yayında GitHub'a commit atar.

backend:
  name: github
  repo: KULLANICI-ADI/deltek-site   # ← yayına alırken GitHub kullanıcı/depo adınız
  branch: main

# Bilgisayarda çalışırken panel doğrudan yerel dosyaları düzenler.
local_backend: true

site_url: https://www.deltek.com.tr
display_url: https://www.deltek.com.tr

# Yeni görseller temiz klasörlere gider; /images/uploads/ artık yalnız eski
# WordPress adreslerinin (/wp-content/uploads/*) 301 hedefi olan orijinalleri
# tutar (bkz. CLAUDE.md → SEO → görsel klasörleri). Blog kendi klasörüne yükler.
media_folder: public/images/icerik
public_folder: /images/icerik

# İki dilli içerik: her yazı için tr/ ve en/ altında birer dosya.
# Slug (dosya adı) iki dilde AYNI kalır — URL yapısı buna bağlı.
i18n:
  structure: multiple_folders
  locales: [tr, en]
  default_locale: tr

slug:
  encoding: ascii
  clean_accents: true
  sanitize_replacement: "-"

collections:
  - name: blog
    label: Blog
    media_folder: public/images/blog
    public_folder: /images/blog
    label_singular: Yazı
    folder: src/content/blog
    create: true
    i18n: true
    identifier_field: baslik
    # DİKKAT: Bu slug canlı sitedeki URL'dir. Mevcut bir yazıyı taşırken
    # deltek.com.tr'deki slug'ın AYNISI girilmeli (bkz. CLAUDE.md).
    slug: "{{slug}}"
    summary: "{{baslik}}"
    sortable_fields: [tarih, baslik]
    fields:
      - { name: baslik, label: Başlık, widget: string, i18n: true }
      # URL yalnız İNGİLİZCE sekmede doldurulur (küçük harf, rakam, tire). Türkçe'de BOŞ
      # kalır: canlı deltek.com.tr adresi dosya adının kendisidir, doluysa site derlenmez.
      - { name: adres, label: "URL (yalnız İngilizce; boşsa dosya adı)", widget: string, required: false, i18n: true, pattern: ['^[a-z0-9-]*$', 'yalnız küçük harf, rakam ve tire'] }
      - { name: seoBaslik, label: "Arama motoru başlığı (boşsa Başlık; 30–65 karakter)", widget: string, required: false, i18n: true }
      - { name: ozet,   label: "Kısa özet — arama sonuçlarındaki açıklama (70–160 karakter)", widget: text, required: false, i18n: true }
      - { name: anahtarKelimeler, label: "Anahtar kelimeler (virgülle)", widget: string, required: false, i18n: true }
      - { name: tarih,  label: Tarih, widget: datetime, format: "YYYY-MM-DD", date_format: "DD.MM.YYYY", time_format: false, required: false, i18n: duplicate }
      - { name: guncelleme, label: "Son güncelleme (isteğe bağlı)", widget: datetime, format: "YYYY-MM-DD", date_format: "DD.MM.YYYY", time_format: false, required: false, i18n: duplicate }
      - { name: kapak,  label: Kapak görseli, widget: image, required: false, i18n: duplicate }
      - { name: kapakAlt, label: "Kapak görseli alt metni (görsel arama için)", widget: string, required: false, i18n: true }
      - { name: video,  label: "YouTube / Vimeo video linki (isteğe bağlı)", widget: string, required: false, i18n: duplicate }
      - name: galeri
        label: Foto galerisi
        widget: list
        required: false
        i18n: true
        label_singular: Fotoğraf
        fields:
          - { name: foto, label: Fotoğraf, widget: image }
          - { name: alt, label: "Alternatif metin", widget: string, required: false }
      - { name: ilgili, label: "İlgili sayfalar (slug listesi)", widget: list, required: false, i18n: duplicate, field: { name: slug, label: Slug, widget: string } }
      - { name: body, label: Yazı içeriği, widget: markdown, required: false, i18n: true }

  - name: sayfalar
    label: Sayfalar
    label_singular: Sayfa
    folder: src/content/sayfalar
    create: true
    i18n: true
    identifier_field: baslik
    # DİKKAT: Bu slug canlı sitedeki URL'dir. Mevcut bir sayfayı taşırken
    # deltek.com.tr'deki slug'ın AYNISI girilmeli (bkz. CLAUDE.md).
    # Sayfalar ve blog yazıları aynı kök adres alanını paylaşır — çakışma
    # olursa site derlenmez.
    slug: "{{slug}}"
    summary: "{{baslik}}"
    sortable_fields: [menuSira, baslik]
    fields:
      - { name: baslik, label: Başlık, widget: string, i18n: true }
      # URL yalnız İNGİLİZCE sekmede doldurulur (küçük harf, rakam, tire). Türkçe'de BOŞ
      # kalır: canlı deltek.com.tr adresi dosya adının kendisidir, doluysa site derlenmez.
      - { name: adres, label: "URL (yalnız İngilizce; boşsa dosya adı)", widget: string, required: false, i18n: true, pattern: ['^[a-z0-9-]*$', 'yalnız küçük harf, rakam ve tire'] }
      - { name: ozet, label: "Kısa açıklama — arama sonuçlarındaki açıklama (70–160 karakter)", widget: text, required: false, i18n: true }
      - { name: anahtarKelimeler, label: "Anahtar kelimeler (virgülle)", widget: string, required: false, i18n: true }
      - { name: menuSira, label: "Menü sırası (boş bırakılırsa menüde görünmez)", widget: number, required: false, value_type: int, i18n: duplicate }
      - { name: menuAd, label: "Menüde görünecek ad (boşsa başlık kullanılır)", widget: string, required: false, i18n: true }
      - { name: kapak, label: Kapak görseli, widget: image, required: false, i18n: duplicate }
      - { name: kapakAlt, label: "Kapak görseli alt metni", widget: string, required: false, i18n: true }
      - name: duzen
        label: "İçerik düzeni"
        widget: select
        required: false
        i18n: duplicate
        options:
          - { label: "Normal (makale akışı)", value: "" }
          - { label: "Logo ızgarası (referans logoları yan yana)", value: "logo-izgara" }
          - { label: "Ürün / teknoloji şablonu", value: "urun" }
          - { label: "İletişim şablonu (ofisler + görsel, form yok)", value: "iletisim" }
          - { label: "Hizmetlerimiz şablonu (kart ızgaraları)", value: "hizmet" }
          - { label: "Referanslar (kayan logo şeridi + metin)", value: "referanslar" }
      - { name: yanMenu, label: "Teknoloji yan menüsünü göster", widget: boolean, required: false, i18n: duplicate }
      - { name: ilgili, label: "İlgili sayfalar (slug listesi)", widget: list, required: false, i18n: duplicate, field: { name: slug, label: Slug, widget: string } }
      - { name: body, label: Sayfa içeriği, widget: markdown, required: false, i18n: true }

      # ── Yalnızca "İletişim şablonu" seçiliyken kullanılır ──
      # Ofis adresleri koddadır (src/data/site.ts OFISLER); buradan yalnızca
      # sayfanın altındaki tanıtım görseli seçilir.
      - { name: gorsel, label: "Sayfa altı tanıtım görseli", widget: image, required: false, i18n: duplicate }
      - { name: gorselAlt, label: "Tanıtım görseli alt metni", widget: string, required: false, i18n: true }

      # ── Aşağıdakiler yalnızca "Ürün / teknoloji şablonu" seçiliyken kullanılır ──
      # <title> ve OG başlığı. Sayfanın görünen başlığı (<h1>) kısaltıldığında
      # uzun, anahtar kelimeli başlık buraya yazılır. Boş bırakılırsa "Başlık"
      # alanı kullanılır — normal sayfalarda boş kalmalı.
      - { name: seoBaslik, label: "Arama motoru başlığı (boşsa Başlık kullanılır)", widget: string, required: false, i18n: true }

      # Başlık banner'ı: sayfa başlığı METİN olarak sembolün yanına basılır
      # (bkz. src/components/SayfaBanner.astro). Seçilirse aşağıdaki "Üst
      # banner görseli" yok sayılır ve ayrı bir başlık satırı basılmaz.
      # Semboller kodda durur (src/assets/banner/*.jpg); yenisi eklenirse bu
      # listeye de yazılmalı, yoksa build durur.
      - name: bannerSembol
        label: "Başlık banner'ı sembolü"
        widget: select
        required: false
        i18n: duplicate
        options:
          - "kazisiz-akilli-altyapi"
          - "kazisiz-boru-yenileme"
          - "yatay-sondaj-boru-surme-cakma"
          - "yonlendirilebilir-yatay-sondaj-camuru"
          - "yonlendirilebilir-yatay-sondaj-delgi-tijleri"
          - "yonlendirilebilir-yatay-sondaj-genisletme-basligi"
          - "yonlendirilebilir-yatay-sondaj-makinesi"
          - "yonlendirilebilir-yatay-sondaj-manyetik-yonlendirme"
          - "yonlendirilebilir-yatay-sondaj-metodu"
          - "yonlendirilebilir-yatay-sondaj-nedir"
          - "yonlendirilebilir-yatay-sondaj-uzerinden-takip"
          - "yonlendirilebilir-yatay-sondaj-yer-belirleme-sistemleri"
          - "yonlendirilebilir-yatay-sondaj-yonlendirme-basligi"
      - { name: banner, label: "Üst banner görseli", widget: image, required: false, i18n: duplicate }
      - { name: bannerAlt, label: "Banner / sembol alt metni", widget: string, required: false, i18n: true }
      - name: bloklar
        label: "Görsel + metin blokları"
        label_singular: Blok
        widget: list
        required: false
        i18n: true
        collapsed: false
        summary: "{{fields.baslik}}"
        fields:
          - { name: baslik, label: Başlık, widget: string, required: false }
          - { name: metin, label: Metin, widget: text, required: false }
          - { name: gorsel, label: Görsel, widget: image, required: false }
          - { name: alt, label: "Görsel alt metni (boşsa blok başlığı, o da yoksa sayfa başlığı)", widget: string, required: false }
          - { name: ters, label: "Görsel sağda dursun", widget: boolean, required: false, default: false }
          - { name: genis, label: "Tam genişlik (görsel üstte, metin altında aynı genişlikte)", widget: boolean, required: false, default: false }
          - name: boyut
            label: "Görsel boyutu (boşsa tam genişlik)"
            widget: select
            required: false
            options: ["", ceyrek, otuz, kirk, elli, yari, kucuk]
      - name: tablolar
        label: "Teknik veri tabloları"
        label_singular: Tablo
        widget: list
        required: false
        i18n: true
        summary: "{{fields.baslik}}"
        fields:
          - { name: baslik, label: Tablo başlığı, widget: string, required: false }
          - { name: aciklama, label: Açıklama, widget: text, required: false }
          - { name: basliklar, label: "Sütun başlıkları", widget: list, required: false, field: { name: ad, label: Başlık, widget: string } }
          - name: satirlar
            label: Satırlar
            label_singular: Satır
            widget: list
            required: false
            fields:
              - { name: hucreler, label: Hücreler, widget: list, field: { name: deger, label: Değer, widget: string } }
      - name: galeri
        label: "Foto galerisi"
        label_singular: Fotoğraf
        widget: list
        required: false
        i18n: duplicate
        fields:
          - { name: foto, label: Fotoğraf, widget: image }
          - { name: alt, label: "Alternatif metin", widget: string, required: false }
